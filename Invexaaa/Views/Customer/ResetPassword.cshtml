@*
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@model SnomiAssignmentReal.Models.ViewModels.ResetPasswordVm

@{
    ViewData["Title"] = "Reset HashedPassword";
    ViewBag.HideHeader = true;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    .auth-wrap {
        max-width: 520px;
        margin: 3rem auto;
        padding: 2rem;
        border: 1.5px solid #0a0a0a;
        border-radius: 16px;
        background: #fff;
    }

    .auth-title {
        font-weight: 800;
        letter-spacing: -.01em;
        margin-bottom: .4rem;
    }

    .auth-sub {
        color: #555;
        margin-bottom: 1.25rem;
    }

    .form-label {
        font-weight: 600;
    }
</style>

<div class="auth-wrap">
    <h2 class="auth-title">Forgot your password?</h2>
    <p class="auth-sub">Enter your email and we’ll send you a reset link.</p>

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">@TempData["Error"]</div>
    }
    @if (TempData["Info"] != null)
    {
        <div class="alert alert-info">@TempData["Info"]</div>
    }

    <form asp-action="ResetPassword" asp-controller="Customer" method="post" novalidate>
        @Html.AntiForgeryToken()
        <div asp-validation-summary="ModelOnly" class="text-danger mb-2"></div>

        <div class="mb-3">
            <label asp-for="Email" class="form-label">Email</label>
            <input asp-for="Email" class="form-control" />
            <span asp-validation-for="Email" class="text-danger"></span>
        </div>

        <button type="submit" class="btn btn-dark w-100">Send Reset Link</button>
    </form>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}
*@